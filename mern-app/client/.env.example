# Vite exposes only VITE_-prefixed vars to the client bundle.
# In development the API is reached through the Vite proxy (see
# vite.config.js), so nothing is required here by default.

# Override the dev-proxy target if your API runs elsewhere:
# VITE_API_PROXY_TARGET=http://localhost:5000
